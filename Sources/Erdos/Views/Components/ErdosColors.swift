import SwiftUI

enum ErdosColors {
    // MARK: - Artifact toolbar counts
    static let documents: Color = .blue
    static let notes: Color = .purple
    static let changedFiles: Color = .orange

    // MARK: - Sidebar
    static let attentionDot: Color = .yellow

    // MARK: - Notes
    static let pinnedIcon: Color = .orange

    // MARK: - Repo status
    static let dirtyIndicator: Color = .orange
}
